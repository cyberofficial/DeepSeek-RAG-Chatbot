# Below is a sample .env file with placeholders for your environment variables specific to OLLAMA_API_URL and MODEL. You need to replace these placeholder values with actual ones based on your configuration or settings in Docker, etc. 
OLLAMA_API_URL=http://127.0.0.1:11434
MODEL=deepseek-r1:8b
#EMBEDDINGS_MODEL=nomic-embed-text
STREAMLIT_SERVER_MAX_UPLOAD_SIZE=1000

# Blocked file extensions for security (used if WHITELIST_EXTENSIONS is not set)
BLOCKED_EXTENSIONS=.exe,.dll,.so,.dylib,.com,.msi,.sys,.jar,.bin

# Optional: Set allowed extensions (if set, only these extensions will be allowed)
# Example: WHITELIST_EXTENSIONS=.txt,.pdf,.doc,.docx